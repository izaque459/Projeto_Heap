import heaqp
import 

def dijkstra(graph):


	X = set()
	H = []
	 
	for v in :
		heapq.heappush(H,)
		
	while H :
		X.add(heapq.heappop(H))
		
    for y in :
        H.remove(y)
        
        heapq.heappush(y)
	 